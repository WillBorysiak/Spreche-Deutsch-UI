import type { NextPage } from 'next';

import PageHeading from '../../components/generic/typography/PageHeading';

const TopTips: NextPage = () => {
	return (
		<section id="top-tips-page">
			<PageHeading text="Top Tips" />
		</section>
	);
};

export default TopTips;
