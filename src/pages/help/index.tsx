import type { NextPage } from 'next';

import PageHeading from '../../components/generic/typography/PageHeading';

const Help: NextPage = () => {
	return (
		<section id="help-page">
			<PageHeading text="Help" />
		</section>
	);
};

export default Help;
