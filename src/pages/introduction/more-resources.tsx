import type { NextPage } from 'next';

import Heading from '../../components/generic/typography/Heading';

const MoreResources: NextPage = () => {
	return (
		<section id="resources-page">
			<Heading text="More Resources" />
		</section>
	);
};

export default MoreResources;
